logging.level.root=warn

# Engines application properties for JPA/Hibernate transaction handling
# Ensure Hibernate can manage auto-commit state so commit does not fail with PostgreSQL
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=false

# Also explicitly disable auto-commit at Hikari connection pool level for safety
spring.datasource.hikari.auto-commit=false
