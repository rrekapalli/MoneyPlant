spring.application.name=moneyplant-engines
# Default engines port set to 8081 to avoid clashing with backend (8080)
server.address=0.0.0.0
server.port=${ENGINES_SERVER_PORT:8081}

# Expose actuator if present
management.endpoints.web.exposure.include=* 
