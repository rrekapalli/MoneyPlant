package com.moneyplant.apigateway.controllers;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

/**
 * Controller for handling authentication-related requests.
 * Note: Basic login and home redirects are now handled by WebConfig view controllers.
 * This controller can be extended for more complex authentication scenarios.
 */
@Controller
public class AuthController {
    // This controller is kept as a placeholder for future authentication-related endpoints
    // that may require more complex logic than simple view controllers.
}
